
// Performing a tsk
function greet(name, lastName) {
  console.log('Hello ' + name + ' ' + lastName);
}

// Caculating a value
function square(number) {
  return number * number;
}

greet('Elysee', 'NIYIBIZI'); // Calling the function

console.log(square(2));

let number = square(2);
console.log(`The square of 2 = ${number}`);