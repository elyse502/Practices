/* An Array is a data structure that we use to represent 
a list of items. */

let selectedColors = ['red', 'blue'];
selectedColors[2] = 1;
console.log(selectedColors);
console.log(`The first Element is: ${selectedColors[0]}`);
console.log(`The number of items or elements in the array is = ${selectedColors.length}`);